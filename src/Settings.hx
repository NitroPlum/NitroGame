package;

// Can't change
final GAME_NAME = "NitroGame";
final REF_WIDTH = 640;
final REF_HEIGHT = 360;
final MAX_ENTITIES = 2048;
final TILE_SIZE = 16;
final MAP_MAX_HEIGHT = 256;
final MAP_MAX_WIDTH = 256;

// User can change
var vsync = true;